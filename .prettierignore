dist
node_modules
.worktrees
package-lock.json
tsconfig.tsbuildinfo
templates
*.csv

# The stylesheet is deliberately written one dense rule per line; reformatting
# it would balloon the file without making the CSS any clearer.
src/styles.css

# Nico Workbench deploy assets are generated/bootstrapped static files. Keep
# their compact form stable instead of letting Prettier rewrite the bundle.
nico-workbench-deploy/

# Media news automation is a compact operational script/workflow maintained as
# deployment infrastructure rather than application UI source.
scripts/update-media-news.mjs
.github/workflows/media-news.yml

# Contract test mirrors exact static deployment strings.
src/mediaIntelligenceContract.test.ts

# Superpowers planning artifacts are process documents rather than app source.
docs/superpowers/

# Existing GitHub Pages deploy workflow is maintained separately from app code.
.github/workflows/nico-workbench-pages.yml
